#!/usr/bin/env bash
set -e

echo "===> Running migrations & seeds"
DISABLE_DATABASE_ENVIRONMENT_CHECK=1 bundle exec rails db:drop db:setup
bundle exec rails db:setup

echo "===> Starting Puma"
exec bundle exec puma -C config/puma.rb