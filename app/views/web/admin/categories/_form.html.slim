= form_with model: [:admin, category], class: 'mt-3', data: { turbo: false } do |f|
  - if category.errors.any?
    .alert.alert-danger
      ul.mb-0
        - category.errors.full_messages.each do |msg|
          li = msg

  .mb-3
    = f.label :name, 'Название', class: 'form-label'
    = f.text_field :name, class: 'form-control', maxlength: 255, required: true, autofocus: true

  = f.submit 'Сохранить', class: 'btn btn-primary me-2'
  = link_to 'Отмена', (category.persisted? ? admin_category_path(category) : admin_categories_path),
            class: 'btn btn-link', data: { turbo: false }
